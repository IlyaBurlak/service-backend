# Swagger API Documentation

## Доступ к документации

После запуска сервера Swagger UI доступен по адресу:

- **Локально**: `http://localhost:3000/api`
- **На продакшн**: `https://your-domain.com/api`

## Использование

### 1. Авторизация

Для работы с защищенными эндпоинтами:

1. Откройте Swagger UI по адресу `/api`
2. В верхней части страницы найдите кнопку **"Authorize"** 🔒
3. Введите JWT токен в формате: `Bearer <your_token>` или просто `<your_token>`
4. Нажмите **"Authorize"**
5. Теперь все защищенные эндпоинты будут работать с вашим токеном

### 2. Получение токена

1. Используйте эндпоинт `POST /auth/login` или `POST /auth/register`
2. Скопируйте `access_token` из ответа
3. Вставьте токен в поле авторизации в Swagger

### 3. Тестирование эндпоинтов

1. Выберите нужный эндпоинт из списка
2. Нажмите **"Try it out"**
3. Заполните необходимые параметры
4. Нажмите **"Execute"**
5. Результат отобразится ниже

## Особенности

- ✅ **Автоматическое сохранение токена** - токен сохраняется между запросами
- ✅ **Валидация запросов** - автоматическая проверка входных данных
- ✅ **Интерактивное тестирование** - можно тестировать API прямо из браузера
- ✅ **Документация в реальном времени** - всегда актуальная документация

## Переменные окружения

Для работы на продакшн сервере установите:

```env
BASE_URL=https://your-production-domain.com
PORT=3000
```

Swagger автоматически определит текущий сервер и добавит его в список доступных серверов.

## Структура документации

- **Авторизация** - регистрация, вход, получение информации о пользователе
- **Пользователи** - CRUD операции с пользователями
- **Проекты** - CRUD операции с проектами
- **Комментарии** - CRUD операции с комментариями
- **Реакции** - CRUD операции с реакциями
- **Тестовые эндпоинты** - тестовые эндпоинты для проверки

## Примеры использования

### Локальная разработка

```bash
npm run start:dev
# Откройте http://localhost:3000/api
```

### Продакшн

```bash
npm run build
npm run start:prod
# Откройте https://your-domain.com/api
```

## Примечания

- Все эндпоинты с 🔒 требуют авторизации
- Токен JWT действителен 1 час (настраивается в `auth.module.ts`)
- Валидация входных данных выполняется автоматически
- Swagger UI работает только в режиме разработки и продакшн (не рекомендуется для production без защиты)

